{
  "name": "buyzaar-monorepo",
  "version": "1.0.0",
  "description": "Buyzaar - Multi-Vendor Smart Shop",
  "private": true,
  "workspaces": [
    "backend",
    "fronend"
  ],
  "scripts": {
    "install-all": "npm install",
    "build": "npm run build --prefix fronend",
    "dev": "npm run dev --prefix backend & npm run dev --prefix fronend"
  }
}
